---
title: Installation
description: Installation guide for our application.
---

Install lokio Assistant and we will speed up the structured development process.
## Installation
To start lokio, you must install it 
<Stepper>
  <StepperItem title="Step 1: Installing Lokio Assistant">
    <Tabs defaultValue="mac" className="pt-5 pb-1">
      <TabsList>
        <TabsTrigger value="mac">Mac (arm64)</TabsTrigger>
        <TabsTrigger value="linux">Linux</TabsTrigger>
        <TabsTrigger value="windows">Windows (x64)</TabsTrigger>
      </TabsList>
      <TabsContent value="mac">
        ```bash
        curl -fsSL https://lokio.dev/in/mac.sh | bash
        ```
      </TabsContent>
      <TabsContent value="linux">
        ```bash
        curl -fsSL https://lokio.dev/in/linux.sh | bash
        ```
      </TabsContent>
      <TabsContent value="windows">
        ```bash
        powershell -c "irm https://lokio.dev/in/win.ps1 | iex"
        ```
      </TabsContent>
    </Tabs>
    Check your lokio version:
    ```bash
    lokio --version or lokio -v
    ```

  </StepperItem>
  

  <StepperItem title="Step 2: Lokio Usage">
    You can take advantage of the lokio-cli features with the following command:
    <Note title="Remember">
      If we have the last directory, then lokio-Cli has only 2 commands init and creation.
    </Note>
  </StepperItem>
</Stepper>